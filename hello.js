// variable message
const message = 'Hello World';

function sayHi(message) {
  // return message parameter
  return message;
}

// call function sayHi and add a message as param
console.log(sayHi(message));